Soundboard assets

Place short MP3/WAV sound effects here. Filenames are used as keywords (without extension).
Example:
- ding.mp3  -> triggers on keyword "ding"
- rimshot.mp3 -> triggers on keyword "rimshot"

Keep effects short (<= 3s) and royalty-free.
